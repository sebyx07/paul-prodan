import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:location');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
